import{TestBed}from'@angular/core/testing';import{of,throwError}from'rxjs';import{describe,expect,it,vi}from'vitest';import{SmartCounsellingApiService}from'../core/smart-counselling-api.service';import{AnalyticsComponent}from'./analytics.component';
const analytics:any={filters:{dateFrom:'2026-08-16',dateTo:'2026-08-22'},overview:{sessions:4,uniqueProspects:3},funnel:[{code:'STARTED',count:4,unit:'sessions'}],outcomes:[],courses:{recommended:[],primarySelected:[],alignment:{}},followups:[],noSuitableCourse:{count:0,dimensions:[]},counsellors:[],options:{branches:[],counsellors:[],courses:[]}};
describe('AnalyticsComponent',()=>{function subject(response:any=of(analytics)){const api={getAnalytics:vi.fn(()=>response)};TestBed.configureTestingModule({providers:[{provide:SmartCounsellingApiService,useValue:api}]});return{component:TestBed.runInInjectionContext(()=>new AnalyticsComponent()),api};}it('loads metrics and applies server default dates',()=>{const{component}=subject();component.ngOnInit();expect(component.metric('sessions')).toBe(4);expect(component.filters.date_from).toBe('2026-08-16');expect(component.width(4,analytics.funnel)).toBe('100%');});it('reloads with a date preset',()=>{const{component,api}=subject();component.preset(30);expect(api.getAnalytics).toHaveBeenCalled();expect(component.filters.date_from).not.toBe('');});it('surfaces API errors',()=>{const broken=subject(throwError(()=>new Error('failed'))).component;broken.ngOnInit();expect(broken.error()).toContain('could not be loaded');});});
