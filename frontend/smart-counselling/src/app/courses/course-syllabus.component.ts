import {ChangeDetectionStrategy,Component,Input} from '@angular/core';import {SyllabusData} from '../core/api.models';
@Component({selector:'sc-course-syllabus',template:`@if(syllabus.status==='NOT_AVAILABLE'){<div class="sc-syllabus-empty"><i class="bi bi-journal-x"></i><p>{{syllabus.message||'Syllabus preview is not available for this course yet.'}}</p></div>}@else{<div class="sc-syllabus"><p class="sc-syllabus-program">{{syllabus.program?.title}}</p>@for(chapter of syllabus.chapters;track chapter.id;let i=$index){<details [open]="i===0"><summary><span>Chapter {{i+1}}</span><strong>{{chapter.title}}</strong><i class="bi bi-chevron-down"></i></summary><ol>@for(topic of chapter.topics;track topic.id){<li><span>{{topic.title}}</span>@if(topic.estimatedTime){<small>{{topic.estimatedTime}} min</small>}</li>}</ol></details>}</div>}`,changeDetection:ChangeDetectionStrategy.OnPush})
export class CourseSyllabusComponent{@Input({required:true})syllabus!:SyllabusData;}
